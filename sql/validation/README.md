# Historical Validation Scripts

This folder contains standalone validation and inspection scripts used during earlier stages of pipeline development.

These scripts are not part of the current orchestrated pipeline.

Current validation is performed inline within the active scripts in:

```text
sql/pipeline/
